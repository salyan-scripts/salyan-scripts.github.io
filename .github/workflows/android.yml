name: Build Nativo Release
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android Resources
        run: |
          mkdir -p app/src/main/res/mipmap-mdpi
          cp icon.png app/src/main/res/mipmap-mdpi/ic_launcher.png 2>/dev/null || touch app/src/main/res/mipmap-mdpi/ic_launcher.png

      - name: Build Universal Release APK
        run: |
          gradle wrapper --gradle-version 8.4
          chmod +x gradlew
          ./gradlew clean assembleRelease

      - name: Get zipalign path
        run: |
          ZIPALIGN=$(find $ANDROID_HOME/build-tools -name zipalign | sort -r | head -n 1)
          echo "ZIPALIGN_PATH=$ZIPALIGN" >> $GITHUB_ENV

      - name: Sign APK
        uses: r0adkll/sign-android-release@v1
        id: sign_app
        with:
          releaseDirectory: app/build/outputs/apk/release
          signingKeyBase64: ${{ secrets.SIGNING_KEY }}
          alias: ${{ secrets.ALIAS }}
          keyStorePassword: ${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: "34.0.0"

      - name: Rename APK to com.salyan.biblia.apk
        run: |
          # Pega o arquivo assinado gerado no passo anterior e renomeia
          mv "${{ steps.sign_app.outputs.signedReleaseFile }}" app/build/outputs/apk/release/com.salyan.biblia.apk

      - name: Upload Final APK
        uses: actions/upload-artifact@v4
        with:
          name: Biblia-Pro-Final
          path: app/build/outputs/apk/release/com.salyan.biblia.apk
